# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

This repository currently contains a single file with an opaque, cryptic name (`source[5GHxhb1U7Lc5jIMpumASbN2teg9dyK5EAazzwnfm1gPKQPTWzczGq9IjR97rOtKMlyv4zFmM2NJRHCem9TsCEplirc.json`) whose content is **not valid JSON** despite the `.json` extension — it appears to be an opaque base64-like blob, not source code or structured data that can be meaningfully parsed or explained.

There is no README, no build tooling, and no other files (beyond `.git`) — there is nothing here that constitutes a codebase to develop against yet. There's a single git commit ("Add files via upload").

## Guidance for future work here

- Before writing any code or documentation for this repo, check with the user about what this repository is meant to become — the current contents give no indication of intended purpose, language, or architecture.
- Do not attempt to "fix" or reformat the existing file as JSON without confirming with the user what it actually is; it may be an intentionally-uploaded binary/encoded artifact rather than broken data.
- Update this file once the repository's actual purpose and structure become clear.
