name: Import Local IPA Signer Base

on:
  push:
    branches:
      - local-ipa-signer
    paths:
      - .github/workflows/import-sylva.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - name: Check out isolated branch
        uses: actions/checkout@v4
        with:
          ref: local-ipa-signer
          fetch-depth: 0

      - name: Import audited MIT source
        shell: bash
        run: |
          set -euo pipefail
          tmp="$(mktemp -d)"
          git clone --depth 1 https://github.com/AntonP29/SylvaSigner.git "$tmp/upstream"
          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          cp -a "$tmp/upstream/." .
          rm -rf .git
          rm -rf .github/workflows/import-sylva.yml

      - name: Commit imported source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Import SylvaSigner MIT source as Local IPA Signer base"
          git push origin HEAD:local-ipa-signer
